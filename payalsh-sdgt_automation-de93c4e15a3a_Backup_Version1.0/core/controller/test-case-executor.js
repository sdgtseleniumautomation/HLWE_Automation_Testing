module.exports = function(testSuite) {
	// - Load test suite using test suite loader
	// - Iterate over the test cases, its steps and load the caselet if overridenExecution is false, if its true, 
	// call the test case's execute method instead
	// - Caselet shall give you element type, action; data will be provided by the test case module
	// - Using the element type, load the element handler using element handler factory
	// - Pass the action and data to the element handler module to execute the step

// var jsaonArray = jsonModules.requireAll();

//  var Map = require("collections/map");
//  var map = new Map();

//  for (var i=0; i<jsaonArray.length; i++){
//  	map.set(jsaonArray[i].name, jsaonArray[i]);
//  }



// describe('login module', function() {
 
//   it('should add a todo', function() {
//     emrLogin.emrLogin(map)
// });


//    it('should add a todo', function() {
   
//        emrNew.emrNew(map)
// //expect(browser.getTitle()).toEqual('Super Calculator');});

// });



}

